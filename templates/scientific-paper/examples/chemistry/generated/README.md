# Generated chemistry assets

This directory is intentionally empty in source releases.

Run `make chemistry-assets` on the preprocessing machine after installing Open Babel.
Review and commit the generated SVG files and `manifest.json` before including them in
a manuscript. The default `make all` build never executes a SMILES converter.
